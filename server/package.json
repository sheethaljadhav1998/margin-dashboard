{
  "name": "margin-dashboard-server",
  "private": true,
  "type": "module",
  "description": "Ingestion, SQLite persistence, and calculation API for the Margin Dashboard.",
  "scripts": {
    "inspect": "node scripts/inspect-data.js",
    "self-check": "node scripts/self-check.js",
    "test": "node --test calc/*.test.js ingest/*.test.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "engines": {
    "node": ">=18"
  }
}
